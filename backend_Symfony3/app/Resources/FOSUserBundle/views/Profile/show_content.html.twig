{% trans_default_domain 'FOSUserBundle' %}


<div>
    <a class="btn-primary" href="{{ path('app') }}">
        mes amis et les autres marcupimalis
    </a>
</div>
<div class="fos_user_user_show">
    <p>{{ 'profile.show.username'|trans }}: {{ user.username }}</p>
    <p>{{ 'profile.show.email'|trans }}: {{ user.email }}</p>
    <p>{{ 'profile.show.age'|trans }}: {{ user.age }}</p>
    <p>{{ 'profile.show.race'|trans }}: {{ user.race }}</p>
    <p>{{ 'profile.show.famille'|trans }}: {{ user.famille }}</p>

    <p>{{ 'profile.show.nourriture'|trans }}: {{ user.nourriture }}</p>

    
    <a class="btn-primary" href="{{ path('fos_user_profile_edit') }}">
        modifier mes information
    </a>

</div>
